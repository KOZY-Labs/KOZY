# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
**/.DS_Store

# env / secrets — NEVER commit Firebase keys or service accounts
.env
.env.*
!.env.example
google-services.json
GoogleService-Info.plist
*serviceAccount*.json
# 소문자 변형도 명시: macOS git은 대소문자 무시로 걸러주지만, EAS/Linux는 대소문자를
# 구분하므로 위 패턴만으로는 admin 키가 빌드 tarball에 포함될 수 있음
*serviceaccount*.json
firebase-adminsdk*.json

# local
*.log

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
*.md
/.claude

# Local build credentials (never commit)
credentials.json
credentials/
build/
/.vscode
/.vercel
sentry.properties
