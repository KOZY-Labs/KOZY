// Listing detail inside the chat stack — opening it here (instead of jumping to the
// home tab) avoids the home video feed flashing and lets back pop to the chat thread.
export { default } from '../../home/[id]';
